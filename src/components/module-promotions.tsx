'use client';

export function ModulePromotions({ moduleId, vendorId, organisationId }: { moduleId: string; vendorId: string; organisationId: string }) {
    return <div className="text-center text-slate-400 py-12">Loading promotions...</div>;
}
