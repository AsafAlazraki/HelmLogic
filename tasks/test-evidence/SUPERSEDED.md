`scripts/gen-smoke-report.mjs` (the standalone Testing & Evidence Report) is SUPERSEDED by `scripts/gen-final-report.mjs` — its methodology blocks (all check families A–J), full per-check appendix (~34,512 rows), browser-suite inventory, nightly cadence and run provenance are now folded into the single `tasks/HelmLogic_MPF_Migration_Evidence_Report.pdf`. Use `node scripts/gen-final-report.mjs` only.
