# v1.23 — Plan (Pipeline + payments + my-work)

**Theme**: Sales pipeline board, per-contract payment schedules, and personal "my work" views.

**Branch**: dev — `claude/app-overview-wKiZ1`. Browser-test every surface on dev before declaring done.

## Story slate (3 shippable)

- **1.7.1 Sales Pipeline Dashboard** — kanban of customers grouped by lifecycle stage (from `organisation.customerDefaults.pipelineStages`). New `/pipeline` page + board component.
- **2.4.3 Payment Schedule** — per-contract payment schedule derived from the contract total + org payment-milestone defaults, with paid/due tracking. Lib + surface in the contract detail sheet.
- **8.1.6 My Quotes / My Customers / My Contracts** — personal views scoped to the current user (createdByUid). New `/my-work` page with three tabs.

## Test plan
- `tests/v1.23-everything.spec.ts` file + `tests/v1.23-browser.spec.ts` against dev.
- Report green before v1.24.
