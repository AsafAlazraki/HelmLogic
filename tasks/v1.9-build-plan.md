# HelmLogic — v1.9 Build Plan

> Status doc for the v1.9 build cycle. Mirrors v1.8 structure
> (`tasks/v1.8-build-plan.md`). Updated as each phase completes.
>
> **Theme:** Quote-lifecycle wrap-up + inline preview. v1.8 shipped
> Send → Lock → Audit → Personalise. v1.9 picks up the 1.1.2
> Compatibility Rules carry-over, unlocks inline PDF preview (1.8.4),
> and adds the remaining lifecycle pieces that don't depend on email
> being live (state machine, multiple scenarios, SharePoint storage).
>
> **Cap status:** ~17 pts (under the 20-pt cap, green margin).
>
> **🚨 Email decisions still pending** — anything that requires
> `NEXT_PUBLIC_EMAIL_SEND_ENABLED` to be live is explicitly OUT of v1.9
> scope (1.3.2 Contract Signing Pack delivery, Viewed-via-open-tracking
> auto-transition on 1.4.1).

---

## Phase

| Phase | Status |
|---|---|
| Pre-flight: capacity sanity + dependency audit | ⏳ pending |
| Phase A — Carry-overs + inline preview (1.1.2 → 1.8.4) | 🔄 in progress |
| Phase B — Quote lifecycle (1.4.1 → 1.1.3) | ⏳ pending |
| Phase C — SharePoint storage (1.3.3) | ⏳ pending |
| Phase D — Pre-merge: release notes + ship flag | ⏳ pending |
| Merge: dev → main + flip `RELEASE_WINDOWS['v1.9'].shipped` | ⏳ pending |

---

## Composition (5 stories, ~17 pts)

### Phase A — Carry-overs + inline preview (lands first)

| # | Story | Pts | Type | dependsOn |
|---|---|---|---|---|
| 1 | **1.1.2** Compatibility Rules + Highfield auto-seed | 3 | feature | — |
| 2 | **1.8.4** Quote preview button (inline PDF render) | 3 | feature | 1.5.0 (shipped v1.8) |

Phase A lands first because:
- 1.1.2 is the long-standing v1.7→v1.8 carry-over — a clean small
  feature to warm up the cycle.
- 1.8.4 was the gating dependency for further preview work and ships
  inline PDF render for proposal-view today.
- **1.8.3 `startsOnNewPage` UI dropped mid-Phase-A**: re-confirmed the
  v1.8 release-notes finding — the v1.7 PDF architecture renders every
  content block on its own A4 page (`proposal-pdf.tsx#renderBlockPages`,
  lines 211/242/255), so the schema field is dormant and 1.8.4 didn't
  change that. Shipping a no-op toggle was specifically rejected once
  in v1.8 and the rendering refactor needed to make the flag mean
  something is ~5 pts of layout work, not the ~1 pt budgeted. Punted
  to v1.10+ (see Punted table). Reclaimed 1 pt buffer.

### Phase B — Quote lifecycle

| # | Story | Pts | Type | dependsOn |
|---|---|---|---|---|
| 3 | **1.4.1** Quote Lifecycle States | 3 | feature | 1.3.1 (shipped v1.8) |
| 4 | **1.1.3** Multiple Quote Scenarios | 3 | feature | 1.3.1 (shipped v1.8) |

**1.4.1** ships the full state machine: Draft → Sent → Viewed →
Accepted / Rejected / Lost / Expired. Manual transition controls in
`proposal-view.tsx` (status badge in header doubles as a state-picker
popover). **Viewed-via-email-tracking auto-transition stays dormant**
until email decisions land — `Viewed` is still reachable manually via
the picker so the state isn't a no-op.

**1.1.3** leverages v1.8's `version + parentQuoteId` fork machinery to
make multiple-version creation an explicit "Create Scenario" action,
not a side-effect of editing-while-locked. Scenarios are sibling
quotes with a shared `parentQuoteId` and a `scenarioLabel` field
(e.g. "Trade-in option", "Cash deal", "Finance bundle").

### Phase C — SharePoint storage

| # | Story | Pts | Type | dependsOn |
|---|---|---|---|---|
| 5 | **1.3.3** SharePoint Quote Storage | 5 | feature | 1.5.0 (shipped v1.8) |

Largest single story in the cycle. Uses the extracted
`render-quote-pdf.ts` to generate the finalized PDF + uploads to a
configured SharePoint folder. Per-org config in
`organisations/{orgId}/sharePointConfig`.

---

## Schema decisions (locked at planning)

| Key | Decision | Why |
|---|---|---|
| **Compatibility rules author** | Engineering writes rules direct to Firestore. Admin UI deferred to v1.10+. | Same call as v1.8 plan. 3 pts vs 8. Highfield auto-seed at v1.9 ship covers the only customer that needs it. |
| **Inline preview transport** | Reuse `@react-pdf` client-side render (`renderQuotePdf()` from 1.5.0). PDF rendered to Blob → object URL → `<iframe>` inside a `Sheet`. | No new SDK; PDF render is already in-page. Same engine guarantees Preview matches Download byte-for-byte. |
| **Preview surface** | Quote builder only (proposal-view.tsx). | Per the 1.8.4 build calibration — content-block editor already has live preview + focus mode from v1.7 story 1.8.7. A second surface would duplicate v1.7. |
| **Lifecycle states** | Full state machine ships (Draft / Sent / Viewed / Accepted / Rejected / Lost / Expired). Manual transition picker in proposal-view header. Email-driven auto-transitions stay dormant. | Avoids rewiring the state enum later when email infra ships. Viewed is reachable manually so the state isn't a dead code-path. |
| **Multiple scenarios** | Reuses v1.8's `version + parentQuoteId` fork machinery. Explicit "Create Scenario" button generates a sibling quote with shared parent + `scenarioLabel`. | No new schema. Minimum-viable UX — sibling list in the parent quote's Activity tab. |
| **SharePoint provider** | Microsoft Graph API via service-principal (OAuth client-credentials). Per-org config in `organisations/{orgId}/sharePointConfig` (siteId, folderPath, tenantId, clientId, clientSecretRef). | Standard enterprise SharePoint integration. Avoids per-user OAuth dance. |
| **SharePoint trigger** | On quote finalize (status flip to a terminal state). Not on every save — keeps SharePoint folder clean. | Aligns with existing `finalize-quote-dialog.tsx` flow. |
| **Email-dependent work** | 1.3.2 Contract Signing Pack + Viewed-via-tracking auto-transition are explicitly punted to v1.10+. | Stakeholder freeze on email-send activation. Revisit when `NEXT_PUBLIC_EMAIL_SEND_ENABLED` decision lands. |

---

## Pre-flight infra

### SharePoint — collaborative setup (Phase C only)

For story 6 (1.3.3) to actually upload files, an Azure app registration
+ service principal must be wired up with `Files.ReadWrite.All` /
`Sites.ReadWrite.All` Graph permissions, and the org admin must drop
the client secret into Firebase Console secrets. **Same pattern as
v1.8's `ADMIN_TASK_email-trigger-setup.md`** — operator handles Azure
+ secrets, agent guides through each step. Will be authored at start
of Phase C.

Risk if not done by Phase C kickoff: 1.3.3 stalls at upload step.
Mitigation: SharePoint write path env-flag-gated
(`NEXT_PUBLIC_SHAREPOINT_ENABLED`) so the rest of v1.9 can ship
without SharePoint provider being live, mirroring v1.8's email-flag
pattern.

---

## Punted to v1.10

These v1.9-Firestore-tagged stories are punted out of v1.9:

| Story | Pts | Reason |
|---|---|---|
| **1.3.2** Contract Signing Pack Generation | 5 | Pack delivery is email-adjacent; hold until email decisions land. |
| **2.1.1** Structured Price Sources | 2 | No carry-over dependency, lower priority. Slot into v1.10 pricing-themed lane. |
| **1.8.3 `startsOnNewPage` UI** | ~1 → ~5 | Schema field dormant under v1.7 PDF architecture (every block already on its own page). 1.8.4's inline-preview path didn't change that. The rendering refactor needed to make the flag actionable is layout work (~5 pts), not the ~1 pt UI slice originally budgeted. Same call v1.8 made — would mislead users if shipped as a no-op toggle. Reclaimed 1 pt buffer. |

0-pt decisions / deliverables (Trade-in valuation rules, Variation
order PDF template, Catalog Manager training, Comms plan, Schedule
training) stay in their respective epics — they're discussion/admin
items, not build work, and don't count against any release cap.

---

## Build commit-by-commit

Filled in as each story ships. Matches the v1.8 commit-by-commit log
format.

```
Commit 1 — v1.9 kickoff
  + tasks/v1.9-build-plan.md (this file, new)
  + CLAUDE.md release-state table — v1.9 row refreshed (🔄 In progress)
  + scripts/survey-v19-backlog.ts removed (one-shot diagnostic)

Commit 2 — 1.1.2 Compatibility Rules + Highfield auto-seed
  + src/lib/compatibility-rules.ts (new) — schema + helpers
  + Highfield auto-seed (one-shot button on /manage, removed in same cycle)
  + 🚨 firestore.rules — compatibilityRules path

Commit 2b — 1.1.2 seed cleanup (one-shot lifecycle complete)
  - src/lib/v19-highfield-compat-seed.ts (deleted)
  - Seed Highfield Compat button + AlertDialog removed from backlog-view.tsx
  - Drops Loader2/ShieldAlert/AlertDialog*/useToast imports no longer used

Commit 3 — 1.8.4 Quote preview button (inline PDF render)
  + Preview button on proposal-view.tsx (between Send Quote + Download PDF)
  + src/components/quote-preview-sheet.tsx (new) — Sheet wrapping iframe
  + Reuses renderQuotePdf() from 1.5.0 (single blob, shared by sheet's Download)
  - content-block-detail.tsx surface DROPPED mid-build: v1.7 story 1.8.7
    already ships an inline `<ContentBlocksPdfPreview>` + focus-mode
    fullscreen on that surface. Adding another button there would
    duplicate v1.7 with no UX gain.

Commit 4 — 1.8.3 calibration: dropped from v1.9
  - Phase A table loses row 3 (1.8.3 UI slice)
  - Punted table gains 1.8.3 row with rationale (schema field dormant
    under v1.7 PDF architecture; 1.8.4 didn't unblock it)
  - Cap drops 18 → 17 pts (1 pt buffer reclaimed)
  - Schema-decisions table loses the `startsOnNewPage UI` row

Commit 5 — 1.4.1 Quote Lifecycle States
  + QuoteStatus enum + transition helper
  + Status badge → popover state-picker in proposal-view header
  + Audit-log entry on every transition
  + 🚨 firestore.rules — onlyStateFieldsChanged() helper

Commit 6 — 1.1.3 Multiple Quote Scenarios
  + Create Scenario button on quote header
  + scenarioLabel field on quote
  + Sibling-list in Activity tab

Commit 7 — 1.3.3 SharePoint Quote Storage
  + tasks/ADMIN_TASK_sharepoint-setup.md (new)
  + src/lib/sharepoint-upload.ts (new) — Graph API wrapper
  + Server-side upload trigger on finalize (Cloud Function or API route)
  + organisations/{orgId}/sharePointConfig schema + /manage UI
  + 🚨 firestore.rules — sharePointConfig path

Commit 8 — Pre-merge: RELEASE_NOTES_v1.9.0.md + ship flag
  + tasks/RELEASE_NOTES_v1.9.0.md (new — same template as v1.8)
  + RELEASE_WINDOWS['v1.9'].shipped = true
  + CLAUDE.md release-state table flipped to ✅ Shipped
  + tasks/v1.9-build-plan.md phase-table flipped to ✅
```

---

## Carry-over freeze list (don't expand mid-cycle)

Per `tasks/CONVENTIONS.md` — "don't expand release scope mid-cycle".
The following are explicitly OUT of v1.9 and stay punted regardless of
how Phase A/B/C feel as we build:

- Contract Signing Pack PDF assembly (1.3.2) — wait for email decision
- Compatibility Rules admin UI (1.1.2 extension) — v1.10+
- 1.8.3 `startsOnNewPage` UI — needs rendering refactor (~5 pts), not the 1-pt slice; v1.10+ when there's a real multi-block layout case
- Viewed-via-open-tracking auto-transition (1.4.1 extension) — wait for email
- Structured Price Sources (2.1.1) — v1.10 pricing lane
- SharePoint per-user OAuth (alternative to service-principal) — never; staying with service-principal

Anything else surfaced mid-cycle goes onto the v1.10 backlog, NOT into
v1.9.
