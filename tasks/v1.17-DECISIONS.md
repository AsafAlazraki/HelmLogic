# v1.17 — Product Decisions

Decisions resolved in v1.17 without shipping new code. Same pattern as `tasks/v1.16-DECISIONS.md`.

## 3.7.8 — Delivered deals + stock dashboard placement

**Decision**: No new dashboard. Surface via existing cards on the Highfield module page.

**Why**:
- The module dashboard already shows "IN STOCK" + "ON ORDER" + "LOCATIONS" + "PROPOSALS" stat cards at the top.
- v1.16/ltaY5TPd shipped an Archive toggle on Recent Proposals that already lists delivered + cancelled quotes.
- Building a separate "Delivered deals" page would duplicate that data and split operator attention.

**Closeout** (v1.18 polish): one-line tooltip on the Archive button: *"Delivered + cancelled quotes live here. Click a card to see the full proposal."*

## 11.3.2 + 11.3.3 NSM-Hub migration

**Status**: still blocked on the `nsm-service-quotation` read service-account. Same block as v1.11 → v1.14 → v1.16 → v1.17.

**Plan**: Carries to v1.18 as a conditional track. If Mark unblocks the service-account this cycle, both stories ship. Otherwise slip again without expanding scope.
