# v1.22 — Plan (Sales workspace shell)

**Theme**: Wrap the v1.21 customer surfaces in a real Sales workspace nav, add the cross-module Contracts view, the margin-threshold admin UI, promotions entry, activity feed, and the content-block layout toggle.

**Branch**: dev — `claude/app-overview-wKiZ1`. Browser-test every surface on dev before declaring done.

## Story slate (7 shippable + 3 NSM-Hub blocked)

### Phase A — Sales workspace (browser-tested)
- **8.1.1 Sales workspace shell + Customers list** — new "Sales" nav group → Customers / Contracts / Reporting. Ties the v1.21 surfaces into the sidebar.
- **8.1.5 Cross-module Contracts view** — `/contracts` page, collectionGroup('contracts') sortable + filterable list. New recursive rule.
- **2.7.1 Margin Threshold Configuration UI** — admin card on /manage to set `organisation.marginThresholdPct` (the v1.19 gate's value). No more Firestore hand-edit.
- **1.7.3 Recent Activity Feed** — component reading the org's recent audit-log events across quotes.

### Phase B — supporting (file-tested + smaller wiring)
- **4.1.1 Promotion Entry (manual)** — promotions admin entry form.
- **2.6.1 Variation Order Document** — variation-order PDF (delta sheet) using pdf-branding tokens.
- **1.8.3 Content block layout controls + "starts on new page" toggle** — schema + toggle on the content-block editor.

### Blocked — NSM-Hub trio (11.3.x), not faked.

## Test plan
- `tests/v1.22-everything.spec.ts` file-based + `tests/v1.22-browser.spec.ts` against dev.
- Extend rules-deployed test for collectionGroup('contracts').
- Report green before v1.23.
