# v1.17 — Phase B Bug Sweep

The v1.17 plan slates 5 bugs to clear, originally targeted at v1.11 and retargeted to v1.17 in close-out. Status as of v1.17 Phase A close:

| Bug | Status | Notes |
|---|---|---|
| HL Error on saving project | **Awaiting repro** | No reliable trigger captured. Once a customer reproduces, instrument the proposal-save path and trace via the Firebase error listener denylist (added in v1.10) so we surface the underlying Firestore write that's failing. |
| $76.82 price difference on RU200KAM between MPF & HL | **Awaiting trace** | Likely a priceLevels rounding mismatch (Yamaha MPF rounds at column, HL re-derives sell via `getPriceForLevel()` and Math.ceil GST). Plan: log the per-step price chain for this specific SKU on next quote-build and diff against the MPF column. |
| Trailer Catalog missing many trailer models | **Data import** | Resolved structurally by v1.17/3.10.2 paste-from-spreadsheet upload + 3.11.3 importer registry. Operator can paste the missing rows from the source spreadsheet and the upsert pipeline lands them. Treating as resolved-by-platform-feature; closeout depends on Mark / Ben running the paste. |
| 11.3.1 Customer reconciliation (NSM → HL) | **Service-account blocked** | Same `nsm-service-quotation` read access still pending. Carries to v1.18. |
| 3.7.8 Delivered deals + stock dashboard placement | **Decision** | Placement decision lands in `tasks/v1.17-DECISIONS.md` (this PR): existing Highfield module dashboard already surfaces "Stock Management" + "Recent Proposals" cards at the top. Delivered deals is a sub-view of the proposals card via the Archive toggle (shipped v1.16/ltaY5TPd). No new placement needed; closeout = a one-line tooltip on the Archive button mentioning "delivered + cancelled quotes live here". Tracked as a v1.18 polish item. |

## Outcome

- **2 of 5 actually clear in v1.17**: trailer-catalog data (resolved by 3.10.2 + 3.11.3) and 3.7.8 placement decision (decision doc).
- **2 of 5 stay parked awaiting repro** (HL save error + RU200KAM delta): can't ship a fix without a reliable trigger. Plan documented for the moment a repro lands.
- **1 of 5 stays blocked on NSM-Hub service-account** (11.3.1): carries to v1.18.

These four "not actually fixed" items are honest. We're not flipping their Firestore status to `shipped`. They stay `planned @ v1.17` for HL save + RU200KAM (awaiting-repro tag), `planned @ v1.18` for 11.3.1, and a `decision` flip for 3.7.8.
