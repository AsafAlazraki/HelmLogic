# v1.11 — Fit-Up Expansion (todo)

Branch: `claude/app-overview-wKiZ1`. v1.11 is the Fit-Up release; nothing else ships in this cycle. The phase-A umbrella build is already in (step 5 selector + finalize snapshot + summary-line PDF). This todo is the EXPANSION pass.

## Phase A — Catalog schema + UI
- [ ] FitUpItem schema: add `category?: string`, `customerDescription?: string`
- [ ] FitUpItemEditor: category text input + customerDescription textarea
- [ ] CSV import: parse `Category` + `Customer Description` columns
- [ ] CSV export: add `Category` + `Customer Description` columns
- [ ] Catalog Manager: category filter chips (dynamic — from items in catalog)

## Phase B — Packages
- [ ] New collection `organisations/{orgId}/fitUpPackages/{id}` — `{name, description, itemIds, createdAt, updatedAt}`
- [ ] `firestore.rules` — `match /fitUpPackages/{packageId} { allow read, write: if isSignedIn(); }`
- [ ] PackageEditor dialog (select items via multi-select chips)
- [ ] Sub-tabs in FitUpCatalogManager: "Items" vs "Packages"
- [ ] Quote selector: render packages strip above items grid; clicking a package toggles all its items on/off

## Phase C — Quote-flow polish
- [ ] Search input in quote selector (filters by name + customerDescription + notes)
- [ ] Category filter chips in quote selector (when categories exist)
- [ ] State: change `selectedFitUpItems: FitUpItem[]` → `selectedFitUpItems: FitUpSelection[]` where `FitUpSelection = { item: FitUpItem, quantity: number, priceOverride: number | null, quoteNote: string | null }`
- [ ] Add a "Selected fit-up items" sub-section under the grid with per-row qty stepper, price override input, per-quote note input
- [ ] Update `toggleFitUpItem` + add `updateFitUpSelection`
- [ ] Update running total: `qty × (override ?? sellPrice ?? cost)`

## Phase D — Snapshot + financials
- [ ] Finalize payload: include `quantity`, `priceOverride`, `quoteNote` on `fitUpSelections[i]`
- [ ] `quote-financials.ts` + `proposal-view.tsx`: respect qty + override in totals
- [ ] PDF summary line unchanged (locked product decision) — but use override when present

## Phase E — Lifecycle status on the quote
- [ ] Add `fitUpStatus: 'pending' | 'scheduled' | 'in-progress' | 'complete'` to quote schema (optional, default `'pending'`)
- [ ] Render status pill on proposal-view (alongside lifecycle pill)
- [ ] Dropdown to change status (audit-logged)

## Phase F — Story retargeting (non-fit-up → v1.12)
v1.11 = fit-up only. Other stories built in this dev cycle move to v1.12.
- [ ] One-shot `V111NonFitUpRetargetButton`:
  - 11.2.1 (Service Quote Flow) v1.11 → v1.12
  - 11.1.3 + 11.1.4 (Service Catalog refinements) v1.11 → v1.12
  - 3.7.3 (Motors Table read-view) v1.11 → v1.12
  - 3.5.1 (Suggestion Approval Queue) v1.11 → v1.12
  - Add umbrella row capturing v1.11 fit-up EXPANSION work
- [ ] Remove `V111FitUpRetargetButton` (already applied earlier)
- [ ] Remove `V111NonFitUpRetargetButton` in same cleanup commit (one-shot lifecycle)

## Phase G — Release docs + flag flip
- [ ] `tasks/RELEASE_NOTES_v1.11.0.md` (fit-up-only narrative + footnote: groundwork shipped for v1.12 service quoting)
- [ ] `tasks/USER_GUIDE_v1.11.0.md`
- [ ] `RELEASE_WINDOWS['v1.11'] = { shipped: true }` + bump `FORWARD_RUNWAY_START` to 12
- [ ] CLAUDE.md release table row for v1.11
- [ ] `.agents/evolution.md` session entry
- [ ] `tasks/SESSION_HANDOVER.md` — new collections (`fitUpPackages`), new fields, new lifecycle status
