# v1.4 Trailers Module — Design Document

> Status: Design phase (this week) — implementation in v1.4 next week
> Decisions baked in: Multi-brand single module (Option A), Excel import, full price levels, no promotions, model.trailerConfig sourced from Trailers module, dealer fit support like Yamaha

---

## 1. Overview

A new module type `trailers` that:
- Aggregates multiple trailer brands (e.g., Mackay, Dunbier, Easytow, Jurgens) under one module
- Becomes an **associated vendor** to boat modules (Highfield) so trailer pricing flows through to boat quotes
- Has its own dealer fit categories (Spare Wheel, Hold Down Straps, Registration, etc.)
- Mirrors the Yamaha workspace UX (Catalog, Pricing Manager, Settings) — minus Promotions

---

## 2. Module Type

### New value
- `moduleType: 'trailers'`

### Module document fields
```
modules/{trailerModuleId}:
  moduleType: 'trailers'
  name: 'Trailers'
  mainVendorId: null                       ← no single main vendor
  trailerBrandVendorIds: string[]           ← NEW — array of trailer brand vendor IDs
  moduleDealerFitCategories: string[]       ← reused — trailer dealer fit categories
  associatedVendorIds: string[]             ← MPF/parts vendors for dealer fit items
  brandCaptainUserId, moduleManagerUserId   ← role assignments
  coverImageUrl: string                     ← module card image (a generic trailers icon)
  stockLocations: string[]                  ← if trailers tracked in stock
```

---

## 3. Vendors

Each trailer brand is its own vendor in `data-warehouse/{vendorId}`:

```
data-warehouse/{trailerVendorId}:
  name: 'Mackay Trailers'
  vendorType: 'Trailer Brand'             ← NEW vendor type
  logoUrl: '...'
  currency: 'AUD'
  shortCode: 'MAC'
```

### Vendor type values (current + new)
- `Boat Brand` (Highfield, Stabicraft, etc.)
- `Motor Brand` (Yamaha)
- `Trailer Brand` ← **NEW**
- `Internal` (Master Price File)

---

## 4. Data Import (Excel)

Each trailer brand has its own Excel file (or sheet). Import flow mirrors MPF:

- User clicks "Import Excel File" in the Trailers workspace Settings
- Selects the brand the data belongs to
- Uploads Excel — each row becomes a doc in `data-warehouse/{trailerVendorId}/dataSets/{name}/rows/{id}`
- Brand association is implicit (vendor = brand)

### Expected Excel columns (per brand — flexible)
- **Required**: Model, Length, Weight (Tare), Capacity (ATM)
- **Pricing**: Cost, NSM Retail / Cash Price, Trade Price, Sub-Dealer Price, Sub-Dealer Excl Price, AUS Sailing Price (matching Yamaha column structure)
- **Specs**: Axles, Suspension, Brakes, Tyres, Drawbar, Hub Type, Lights
- **Optional**: Image URL, Notes, Compliance details

Same flexible field-name handling as Yamaha — `getMotorName`-style helpers that try multiple casings.

### Row document structure
```
data-warehouse/{trailerVendorId}/dataSets/{datasetId}/rows/{rowId}:
  Model: 'Mackay 4.5m Single Axle Braked'
  Length: 4.5
  Weight: 250
  Capacity: 750
  Cash Price: 3500
  Trade Price: 3200
  ...
  // Built at fetch time:
  priceLevels: {
    hull_cash: 3500,
    hull_trade: 3200,
    hull_subdealer: 3200,
    hull_subdealer_excl: 3200,
    hull_aus_sailing: 3200,
  }
  sellPriceExclGst: 3500    ← primary, defaults to hull_cash
  costPrice: 2400           ← from Cost column
  brandVendorId: '<vendorId>'   ← injected at fetch for grouping
  brandName: 'Mackay Trailers'  ← injected at fetch for badges
```

---

## 5. Workspace UI — TrailersWorkspace

New component: `src/components/trailers-workspace.tsx`

Modeled directly on `YamahaMotorWorkspace` with these tab differences:

### Tabs
- **Catalog** (default)
- **Pricing Manager**
- **Settings**
- ❌ NO Promotions tab (per user)

### Catalog Tab

**Header**:
- Search bar (filters across all brands)
- Brand selector dropdown: "All Brands" / "Mackay" / "Dunbier" / etc.
- Sort: Length / Weight / Price / Name

**Grid**:
- Trailer cards grouped by brand (when "All Brands" selected) OR flat grid (when single brand selected)
- Each card shows:
  - Trailer image (or Truck icon placeholder)
  - **Brand badge** (top-left, brand-coloured)
  - Model name
  - Length + Capacity badges
  - Price (at current price level)
  - Click → opens Trailer Detail Sheet

### Trailer Detail Sheet (side panel ~480px, like Motor Detail)
- Hero image
- Brand logo + name
- Quick stats: Length, Weight, Capacity, Price
- Specifications grid (all spec fields)
- **Dealer Fit Options** section at bottom — same Master Data Browser as Yamaha (uses `moduleOnly` so only shows the Trailers module's own DF categories)

### Pricing Manager Tab

**Sub-tabs per brand**: Mackay / Dunbier / Easytow
- Each sub-tab renders `MasterPriceFileWorkspace` scoped to that brand's vendor
- Same editable cell UX, Excel import, GST-aware pricing
- Inc GST values rounded to whole dollars (inherited from v1.2.1)

### Settings Tab

- **Trailer Brands**: Add/remove trailer brand vendors (writes to `trailerBrandVendorIds`)
- **Associated Vendors**: MPF vendors for dealer fit data
- **Dealer Fit Categories**: `ModuleDealerFitManager` with `fieldName="moduleDealerFitCategories"`, title "Trailer Dealer Fit Categories" (e.g., Spare Wheel, Hold Down Straps, Registration, Lock, Wheel Brace)
- **Module Roles**: Brand Captain, Module Manager

---

## 6. Quote Builder Integration (Step 4)

This is where it gets clever. Boat modules (Highfield) reference the Trailers module for trailer pricing — AND each boat model has its own assigned trailers (mirroring how Motor Options work today).

### How motors work today (the pattern we're copying)
- Each boat model has `motorOverrides: { Single: { hiddenIds, manualIds }, Twin: {...} }`
- In the **boat model editor**, there's a "Motor Options" tab that lets the user browse the Yamaha catalog and assign which motors are available for THIS boat
- In the **quote builder Step 3**, only those assigned motors appear
- Standard accessories on each motor auto-select

### How trailers will work (parallel pattern)
- Each boat model gets `trailerAssignments[]` — array of trailer configurations assigned to this boat
- New "Trailer Options" tab in the boat model editor lets the user:
  - Browse the Trailers module catalog (same UX as Motor Options)
  - Pick which trailers are available for THIS boat model
  - For each assigned trailer: pre-configure dealer fit selections that come with it
- In the **quote builder Step 4**, only the assigned trailers appear
- Pre-configured dealer fit auto-ticks when the trailer is selected

### Boat module setup
- Add the Trailers module's `trailerBrandVendorIds` to the Highfield module's `associatedVendorIds`
  - OR introduce a new field `trailersModuleId` on boat modules pointing at the Trailers module

### Boat model schema additions
```
data-warehouse/{boatVendorId}/ranges/{rangeId}/models/{modelId}:
  trailerAssignments: [
    {
      id: 'assignment-uuid',
      brandVendorId: 'mackay-vendor-id',
      brandName: 'Mackay Trailers',
      trailerId: 'trailer-row-id',           ← references Trailers module catalog row
      isDefault: true,                        ← one default per boat (recommended)
      preConfiguredDealerFit: [               ← dealer fit items that come standard
        { dealerFitSelectionId: '...', isStandard: true },
        { dealerFitSelectionId: '...', isStandard: false }
      ]
    },
    ...
  ]
  trailerOverrides: {                         ← optional global hide list
    hiddenIds: [],
    manualIds: []
  }
```

### Boat Model Editor — New "Trailer Options" tab

Located in `model-configuration-editor.tsx`, parallel to the existing "Motor Options" tab.

**UI structure**:
- Header: "Trailer Configurations for {Model Name}"
- "Browse Trailers" button → opens trailer browser dialog (full Trailers module catalog with brand filter)
- List of currently assigned trailers as cards, each showing:
  - Brand badge, trailer name, length/capacity/price
  - "Set as Default" radio (one default per model, pre-selected in quote builder)
  - "Configure Dealer Fit" button → opens dealer fit picker scoped to this assignment
  - "Remove" button
- Trailer browser dialog UX mirrors the Yamaha "Manage Engines" flow

**Dealer fit configuration per trailer assignment**:
- Pre-configured selections come from the Trailers module's `moduleDealerFitCategories`
- For each category, user picks which dealer fit selections come standard with this trailer on this boat
- Each can be marked Standard (auto-ticked in quote) or Optional (shown but unticked)
- Example: "CL340 + Mackay 4.5m" → Standard: Spare Wheel, Hold Down Straps; Optional: 12mo Registration

### Quote Builder Step 4 changes

**Before**: Step 4 shows `model.trailerConfig` (single recommended trailer per model)

**After**:
1. Load `model.trailerAssignments[]` for the selected boat model
2. Show trailer selection cards in Step 4 (same UX as motor selection grid):
   - Default trailer is pre-selected
   - Cards show brand badge, name, price (resolved at current price level)
   - User can switch to any other assigned trailer
3. After trailer selected:
   - Hero card appears with brand + specs (mirrors motor hero card)
   - **Pre-configured dealer fit auto-ticks** for that trailer assignment
   - "Choose Another Trailer" button to switch (just like motors)
   - Trailer Factory Options + Custom Trailer Notes (existing v1.3 features) still work
   - Additional Trailer Dealer Fit section shows extra options from Trailers module DF categories (user can add more beyond the pre-configured ones)
4. Auto-ticked items can be unticked by the user

### Pricing resolution
- Trailer `priceLevels` flows through `getPriceForLevel` (same as motors)
- Sub-dealers automatically get Trade pricing on trailers
- Hero card price changes when price level selector changes (same fix as motors v1.2.1)

### Snapshot at finalize
```
users/{uid}/quotes/{qid}.trailer:
  assignmentId: 'assignment-uuid'
  brandVendorId: 'mackay-vendor-id'
  brandName: 'Mackay Trailers'
  trailerId: 'trailer-row-id'
  name: 'Mackay 4.5m Single Axle Braked'
  sellPriceExclGst: 3200            ← resolved at price level
  costPrice: 2400
  imageUrl: '...'
  options: [...]                    ← existing trailer options
  customOptions: [...]              ← existing custom trailer notes
  preConfiguredDealerFit: [...]     ← snapshot of what was auto-ticked from assignment
  // Specs snapshotted for proposal/PDF
  length: 4.5
  capacity: 750
  axles: 1
```

---

## 7. Dealer Fit — Where Categories Live

### Current state (v1.3)
- Boat module has `trailerDealerFitCategories[]` (added in v1.3)
- Quote flow Step 4 reads from boat module's field

### New state (v1.4)
- Trailers module has its OWN `moduleDealerFitCategories[]` (the standard field)
- Quote flow Step 4 reads from the **Trailers module** if the boat module is associated to a Trailers module
- Falls back to boat module's `trailerDealerFitCategories` if no Trailers module is linked
- This lets each org configure trailer DF categories ONCE on the Trailers module rather than per boat module

### Migration
- Existing `trailerDealerFitCategories` on boat modules can be migrated to the new Trailers module's `moduleDealerFitCategories` when the Trailers module is created
- Or kept as a per-boat-module override

---

## 8. Pricing Workspace (Same as Boat Pricing)

Each trailer brand gets the full v1.2.1 pricing treatment:
- Cost + freight + handling + pre-delivery → landed cost
- Margin → sell ex GST
- All price levels (hull_cash, hull_trade, hull_subdealer, hull_subdealer_excl, hull_aus_sailing)
- Inc GST rounded UP to whole dollars
- Publish writes `priceLevels` object to each trailer row

This works because we reuse `MasterPriceFileWorkspace` per brand sub-tab.

---

## 9. Implementation Phases (next week)

| Phase | Scope | Files |
|-------|-------|-------|
| 1 | Module type + vendor type enums | Add `trailers` to module type selector, `Trailer Brand` to vendor type. Add `trailerBrandVendorIds` to module schema. |
| 2 | TrailersWorkspace skeleton | New `src/components/trailers-workspace.tsx` based on YamahaMotorWorkspace |
| 3 | Catalog tab | Brand grouping/filter, trailer cards, detail sheet with dealer fit |
| 4 | Pricing Manager tab | Per-brand sub-tabs rendering MasterPriceFileWorkspace |
| 5 | Settings tab | Brand management (add/remove), DF categories, role assignment |
| 6 | Excel import per brand | Reuse MPF import flow with brand selector |
| 7 | Boat Model Editor "Trailer Options" tab | Browse Trailers catalog, assign trailers to boat models, configure pre-set dealer fit per assignment |
| 7b | Quote builder Step 4 | Show only assigned trailers from `model.trailerAssignments`, auto-tick pre-configured dealer fit, "Choose Another Trailer" between assigned trailers |
| 8 | Pricing flow-through | priceLevels mapping for trailer rows, sub-dealer auto-trade |
| 9 | Finalize payload | Snapshot brand, brand price level, trailer specs |
| 10 | Playwright tests | New `tests/trailers-module.spec.ts` |
| 11 | Documentation | Release notes, handbook update |

---

## 10. Open Questions for Implementation

These can be answered during build, not now:

- **Brand badge design**: Per-brand colour, or generic primary?
- **"Choose Another Trailer" UX**: Full-screen browser, side panel, or in-place expansion?
- **Multiple trailers per quote**: Out of scope for v1.4 — assume one trailer per quote (matches current v1.3 behaviour)
- **Trailer-specific Standard Features dialog**: Each trailer might have its own Standard Features list — same dialog pattern as boats?
- **PDF section per trailer brand**: v1.3 introduced section PDFs (Boat/Motor/Trailer/DealerFit). Should the Trailer section PDF be per-brand or unified?

---

## 11. Files That Will Change

### New files
- `src/components/trailers-workspace.tsx`
- `src/components/trailer-detail-sheet.tsx` (or inline in workspace)
- `src/components/trailer-browser-dialog.tsx` (full Trailers catalog browser, used in both Boat Model Editor and Quote Builder)
- `src/components/trailer-options-editor.tsx` (the new "Trailer Options" tab in the Boat Model Editor — parallel to motor-options.tsx)
- `src/components/trailer-assignment-dealer-fit-picker.tsx` (configures pre-set DF per trailer assignment)
- `tests/trailers-module.spec.ts`
- `testing/v1.4/release-notes.md` (and other v1.4 testing artifacts)

### Modified files
- `src/app/(app)/modules/[id]/page.tsx` — handle `trailers` module type, render TrailersWorkspace
- `src/app/(app)/modules/add/page.tsx` — add "Trailers" option to module type selector
- `src/components/highfield-quote-flow.tsx` — Step 4 reads `model.trailerAssignments`, shows assigned trailers, auto-ticks pre-configured dealer fit
- `src/components/highfield-model-editor.tsx` — add "Trailer Options" tab (parallel to existing tabs)
- `src/components/model-configuration-editor.tsx` — wire up trailer-options-editor.tsx
- `src/components/finalize-quote-dialog.tsx` — trailer payload includes brand fields, assignmentId, preConfiguredDealerFit
- `src/components/dealer-fit-options.tsx` — handle trailer module's own categories

### Firestore schema additions
- `modules/{id}.trailerBrandVendorIds: string[]`
- `data-warehouse/{vendorId}.vendorType: 'Trailer Brand'`
- Quote payload `trailer.{brandVendorId, brandName, trailerId}`

### Firestore rules
- No new collection paths needed — `modules` and `data-warehouse` already covered
- May need to verify the `Trailer Brand` vendorType passes existing rule patterns

---

## 12. Testing Strategy

### Manual test cases (for testing/v1.4/test-cases.md)
- Create new Trailers module (admin)
- Add trailer brands as vendors
- Import Excel for each brand
- Verify catalog shows brand-grouped trailers
- Edit pricing for one brand → verify totals update
- Add trailer DF category, create selection via Master Data Browser
- **Open Highfield CL340 model editor → Trailer Options tab → Browse Trailers → assign 2-3 trailers (e.g., one Mackay, one Dunbier)**
- **Set one as default, configure pre-set dealer fit (mark Spare Wheel as Standard, 12mo Rego as Optional)**
- **Verify saves to `model.trailerAssignments` in Firestore**
- Build quote on CL340 → Step 4 shows ONLY the assigned trailers (default pre-selected)
- Verify pre-configured dealer fit auto-ticks when trailer selected
- "Choose Another Trailer" → swap to other assigned trailer → DF re-evaluates
- Verify CL340 quote does NOT show trailers assigned to a different model (e.g., SP390)
- Sub-dealer view → trailer auto-shows Trade price
- Finalize → verify quote payload has brand fields, assignmentId, preConfiguredDealerFit snapshotted

### Playwright coverage
- Trailers workspace loads with brand filter
- Brand selector changes catalog grid
- Trailer detail sheet opens with dealer fit section
- Pricing Manager shows per-brand sub-tabs

---

**End of Design Document.** Once this is approved and we have brand examples + sample Excel data, implementation kicks off in v1.4 branch.
