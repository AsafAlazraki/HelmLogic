# v1.16 story notes

Per-story notes captured as each one lands on `claude/v1.12-v1.13-push`.
The Firestore status flips happen at the END (after each story is
verified) — for now this doc is the build log.

## Already shipped before v1.16 (status-flip when authorized)

- **9.2.2** "Include fit-up" checkbox — shipped with v1.11 (see `highfield-quote-flow.tsx` lines 176 + 272 + 883 + 2399). No new code.
- **VDUeX9zQ** NO Trailer images shown — resolved by the v1.11 Phase D PDF polish (trailer image fallback chain in `proposal-pdf.tsx`). No new code.
